

const initState:Object = {
    loginResponse:"",
    }
     


    const AuthReducer = (state:Object=initState, action:any) =>{
        switch(action.type){
           
                default:
                    return state
    
        }
    }
    
    export default AuthReducer;
